# Node
node_modules/
*.log
logs/

# 构建产物
dist/
build/
*.tsbuildinfo

# 环境与密钥(永不提交)
.env
.env.*
!.env.example

# SQLite 自有数据(可重建)
code/backend/data/

# impeccable 本机私有配置
.impeccable/config.local.json
.impeccable/cache/

# 测试
coverage/
.vitest-cache/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# pnpm-lock.yaml 必须提交,不要忽略

# 规划文档含内网拓扑与威胁模型,不进公开仓库
.planning/

# Pake 打包产物
*.dmg

# 看板娘模型:用户自己下载/购买的资产,不可重建,放 ~/.xuanji/live2d 不进版本库。
# 原型页 wiki/design/prototype-live2d.html 需要它们才能打开,准备办法见该文件开头注释。
# (frontend/public/live2d/ 下的 Cubism Core 是例外:产品运行必需,且官方
#  RedistributableFiles.txt 明确允许随产品再分发。)
wiki/design/live2d-assets/
