# Product

## Register

product

## Users

唯一用户是作者本人:数据/风控分析师、Claude Code 重度用户,常年深色终端环境、大量夜间时段使用。日常并行运转数十个 Claude 会话(交互 + 后台)、40+ 技能、多个定时任务,分布在 8+ 个活跃项目里。使用场景是「深色终端旁边常驻的一块驾驶舱屏」:扫一眼知道机群状态,出手即派发任务,全程尽量不碰鼠标。

## Product Purpose

璇玑(xuanji)把散落在 `~/.claude/` 里的个人 AI 生产数据聚合成唯一的本地 Web 入口:观测(项目总览、会话看板、token 成本)、派发(经 Agent SDK 的对话式任务下发与审批)、管理(技能启停、经验沉淀、定时任务)。取代「开七个终端敲 `claude agents` / 翻 jsonl / 记不清哪个任务卡住了」的碎片化巡检。成功的样子:每天打开它的次数超过直接翻 `~/.claude` 的次数;blocked 的会话在一分钟内被看到并处理。

命名出自《尚书·舜典》「在璇玑玉衡,以齐七政」——观测与校准天体的仪器,对应产品的观测与调度职责。

## Brand Personality

沉静、精密、克制。「夜间观象台」:近黑底上的玉色仪表,数据是唯一的主角,界面本身隐入黑暗。情绪目标是**掌控感**——不是兴奋,不是愉悦,是「一切尽在掌握」的安静笃定。

## Anti-references

- SaaS 营销风:渐变文字、玻璃拟态、hero-metric 大数字卡、装饰性动效——这是工作台,不是落地页。
- 大面积亮白底:用户在深色终端环境里,白屏刺眼即失败。
- Grafana 式图表墙:密集仪表不等于信息量,每个图表必须回答一个具体问题。
- 通用 admin 模板感(AntD/Bootstrap 默认样式堆砌):熟悉感要靠精确的层级与词汇挣来,不是靠组件库默认值。

## Design Principles

1. **数据可信优先**:所有数字带口径(数据源、聚合方式悬停可查);只读是默认姿态,写操作显式、带确认、可逆。
2. **键盘优先的驾驶舱**:高频路径(切视图、选会话、进派发、返回)全键盘可达,手感对齐 `claude agents` TUI;鼠标是备选不是前提。
3. **状态色即语义**:玉=运行、琥珀=等待输入、蓝=完成/信息、红=错误/熔断——状态色只表状态,永不装饰;项目分类色锁明度转色相,与状态色分守两套词汇。
4. **信息密度服务扫视**:可变文案限行截断、全文在下钻层;层级靠明度阶梯而非颜色堆砌;一屏内回答「现在有什么需要我」。
5. **工具消失于任务**:进入即聚焦、来路可返回、动效只表状态(150-250ms);任何让用户停下来想「这界面怎么用」的瞬间都是缺陷。

## Accessibility & Inclusion

- 正文对比度 ≥ 4.5:1,大字号 ≥ 3:1(深色底上以 OKLCH 明度阶梯保证)。
- 全部动效尊重 `prefers-reduced-motion`(降级为瞬时切换)。
- 状态与分类信息永不 color-alone:状态色配文字标签,项目色配项目名,图表配图例与悬停数值。
- 键盘可达性:焦点态可见(玉色 outline)、快捷键不劫持输入控件、Esc 逐层退出。
