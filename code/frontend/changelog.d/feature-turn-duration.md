### 新增

- **轮次耗时**：派发页状态条在回合进行中实时累计「已用 1m07s」，回合结束定格为「本轮 1m07s」，同一数字同时写进该轮 Claude 消息头（紧跟时间戳），翻历史即可看出哪几轮是重活。

### 变更

- **状态条让位规则**：状态文字变长后整行放不下时，改由会话标识先收缩（它有 ellipsis 兜底），状态里的耗时与成本优先保完整；实在放不下时状态文字末尾给出省略号并由悬停提示显示全文，不再把 `$0.22` 截成看着像 `0.2` 的半个数字。
