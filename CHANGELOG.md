# Changelog

本文件记录璇玑(xuanji)项目的所有重要变更。
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

## [1.7.0] - 2026-08-21

### 新增
- **消息发送时间**：聊天流与回放的每条对话消息显示发送时间，会话跨天处插入日期分隔线。
- **粘贴图片派发**：派发输入框支持 ⌘V 粘贴图片随消息发送，可预览、移除、开灯箱看原图。
- **`/resume` 弹窗搜索**：恢复已关闭会话弹窗支持打字过滤，会话名与 sessionId 均可匹配。

### 变更
- **派发输入框自适应高度**：长文本输入时高度随内容抬高，超过上限后转输入框内部滚动。
- **待办自动完成**：挂靠会话被归档或从看板消失时，进行中的待办自动转「已完成」。

### 修复
- **修复待验收角标不熄灭**：接回后端仍存活的派发会话后，看板右上角的「待验收」标记正常熄灭。
- **修复接回会话丢历史**：后端重启后接回会话时，重启前的历史对话不再消失。
- **修复待办「继续」丢上下文**：进行中的待办改为续接原会话，找不回时才回落全新派发。
- **修复「来自待办」横幅残留**：横幅不再跨会话悬浮，也不会把待办错绑到无关派发会话。

## [1.6.0] - 2026-08-14

### 新增
- **待办全键盘操作**：方向键选中、空格勾选完成、Enter 开工、E 键或双击就地改标题、⌫ 删除，全程可键盘完成。
- **压缩卡片**：会话回放与派发页续接时，压缩位置渲染为可展开的「上下文已压缩」卡片，显示触发方式、token 数与耗时。
- **Weekly 限额模型级配比**：用量条新增当前模型（如 Fable）的独立周利用率双轨显示，与 all models 轨共用同一重置时刻。
- **会话内查找**：会话回放抽屉与派发聊天页支持 ⌘F/Ctrl+F 查找已渲染内容，含大小写/正则切换与折叠卡片命中定位。
- **打包脚本**：新增 `build-app.sh` 固化 Pake 打包命令与版本硬校验，防止 3.x 导致壳内白屏。

### 变更
- **桌面壳图标**：macOS 应用图标换成璇玑品牌矢量图标，Dock 显示尺寸对齐系统应用。

### 修复
- **待办勾选框对比度**：深色底下勾选框描边太淡，现改用更清晰的颜色。
- **修复壳内看板按键失灵**：切回看板后空格/回车/方向键不再被隐藏的输入框吞掉。
- **修复壳内改动不生效**：桌面壳不再读缓存里的旧版前端，重新构建后重开即最新。
- **修复版本号显示滞后**：左上角版本号改为构建时注入，不再停留在旧版本号。

## [1.5.1] - 2026-08-11

### 修复
- **修复看板空格进入**：按空格进不去只能回车的卡片现在一按即入，点击选中的卡片也不再跳回首卡。
- **修复「待验收」角标缺失**：盯着派发页跑完的会话，首次进「验收中」列时右上角角标不再缺失。
- **修复加粗链接点不动**：输出里 `**网址**` 写法的链接不再把星号吞进地址，点击可正常跳转。

## [1.5.0] - 2026-08-06

### 新增
- **「待办」模块**：随手记下临时想法并按日分组筛选，⌘J 可从任意视图纯键盘速记，回顾时一键进派发会话开工。
- **「总结」模块**：派发页 ⚑ 一键把当前会话沉淀成收口卡，新视图里按状态筛选、搜正文、直连会话回放。
- **看板「验收中」列**：跑完有新产出的会话停在此列，须显式继续、挂起或归档才离开，挂起后再有新产出会自动回来。
- **会话卡片拖拽归档**：空闲卡拖到「已完成」即手动归档，已完成卡悬停 ↩ 可撤销。
- **`/clear` 命令**：派发框输入即清空上下文另起一轮，工作目录与模型等设置保持不变。
- **侧栏前后切换快捷键**：⌥⌘←/→ 沿侧栏顺序切换视图，长按可连续跳转。
- **`preview.sh` 一键验收环境**：起一套端口与数据都同生产隔离的实例，多 worktree 可并行。

### 变更
- **看板收到四列**：「等待输入」并入「进行中」，验收中改用中密度卡，空闲降级为默认只展示最近 3 条的停车场。
- **用量条**：三条用量轨加厚到可直接读出比例，配额重置倒计时也从悬停提示改为常驻显示。
- **打字机追赶窗口拉长**：慢流下放字速率贴合到达速率，不再「打一段卡一下」。
- **周报草稿改以任务总结为主体**：按卡片的问题、结论与残留改写，残留据实汇进下周待办。
- **⌘⏎ 不再发送草稿**：⌘⏎ 归任务总结，发送仍是裸 Enter、换行仍是 Shift+Enter。
- **浏览器标签页图标**：换成璇玑玉璧矢量 favicon，颜色对齐 DESIGN.md 玉色 token。

### 修复
- **修复桌面壳外链**：Pake 壳内点击链接可唤起系统浏览器，触摸板轻触一次即跳转。
- **修复链接尾部误吞**：中文正文里的裸 URL 不再把全角括号与后续中文一并吞进链接。
- **修复思考卡收起后不再跟滚**：收起导致的滚动不再被误判为用户上翻，画面继续跟到底。
- **修复验收中卡片按钮失灵**：点卡内按钮时不再被误判为拖拽而吞掉首次点击。
- **修复看板状态跨轮询污染**：挂起或归档后不再出现卡片纹丝不动、刷新状态回弹。
- **修复催办角标误挂**：「待验收」角标只对验收中的卡生效，空闲与已完成不再喊你。
- **修复待办速记输入**：选完项目后回车能正常创建，中文选词的回车与 Tab 不再被当作保存或选项目。
- **修复续接会话无法总结**：可用性判据改看有无可收口上下文，续接会话按钮不再置灰。

## [1.4.0] - 2026-07-29

### 新增
- **模型思考过程展示**：派发台实时展示 Claude 推理过程，可折叠回看并显示耗时。
- **思考深度可选**：派发会话创建时可指定 effort 档位，opus-5 默认 low。
- **1M 上下文模型**：模型列表新增 `claude-opus-5[1m]`，上下文窗口 1M。
- **`/compact` 压缩上下文**：派发框输入 `/compact` 即可压缩当前会话上下文。
- **派发页快捷键 ⌘M / ⌘D**：分别直接唤起切换模型与切换工作目录弹窗。

### 变更
- **默认模型升级 opus-5**：派发模型列表更新至 opus-5，新会话默认使用。
- **流式输出打字机平滑**：回复逐字匀速放出并自动跟随滚底，不再成块蹦字。
- **流式 markdown 实时渲染**：流式期间即时渲染 markdown，长会话不再卡顿。
- **应用图标统一玉璧**：安卓与桌面版图标换为玉璧剪影并修正尺寸偏小。
- **APK 版本号对齐发版号**：安装界面显示的版本与璇玑发版号一致。

### 修复
- **修复派发会话缺失系统提示**：补回 Claude Code 预设，行为与终端 CLI 对齐。
- **修复上下文百分比失真**：分母不再写死 200K，改按模型真实窗口计算。
- **修复提问卡重复渲染**：提问上方多出的原始 JSON 工具卡已去掉。
- **修复侧栏缺失品牌图形标**：玉璧剪影与 favicon 此前只存在于设计稿。
- **修复共享库经验标签显示问号**：`cross-project` 类型未进白名单被判为未知。

## [1.3.0] - 2026-07-17

### 新增
- **`/model` 命令弹窗**：派发页输入 `/model` 唤起模型切换弹窗，模糊搜索交互与 `/wd` 同款。
- **移动端响应式布局**：≤430px 视口改用触屏优先布局，底部 tab 导航 + 单列卡片流，桌面不受影响。

### 变更
- **命令弹窗体验统一**：三个命令弹窗选中态统一玉色描边环，`/resume` 打开同步失焦对齐 `/wd`。
- **工具调用卡片按类别着色**：ToolCard 按行为类别区分颜色，不再统一蓝色，出错状态仍优先显红。

### 修复
- **修复桌面壳内链接点击无响应**：WKWebView 吞新窗口请求，改探测 `window.open`，失败走后端 `/api/open-url`。
- **修复 `/wd` 模糊搜索排序**：输入项目名前缀时目标项目常被无关项目压到下面，改为短名与路径分离的分层打分排序。
- **修复 `/wd` 弹窗搜索框不聚焦**：正式 App(WKWebView)里弹窗打开后光标仍留在派发输入框，改为弹窗内聚焦并加轮询兜底。

## [1.2.0] - 2026-07-16

### 新增
- **Android 原生壳**：新增可安装的安卓 App,界面远程加载并随后端更新自动生效,不必重新打包分发。
- **派发页输入框历史回溯**：↑/↓ 键回溯当前会话发送过的 prompt,回到最新一条后恢复未发送草稿,不跨会话共享。
- **侧栏品牌图形标**：侧栏新增璇玑玉璧剪影 logo 替代纯文字品牌区,favicon 同步更新。
- **`/wd` 工作目录切换弹窗**：派发框输入 `/wd` 唤起切换工作目录弹窗,支持模糊搜索与键盘选择。

### 修复
- **修复后台探索子代理误标空闲**：派发后台子代理运行期间,会话看板曾把顶层轮次结束误判为空闲,现按后台任务真实存活状态显示。
- **修复定时任务模型下拉缺 fable**：任务弹窗模型列表未同步派发页选项,现保持一致。
- **修复壁纸玻璃档下拉菜单透明穿透**：开启壁纸玻璃模式后下拉菜单变透明,能看到下方内容穿透,现固定为不透明。
- **修复下拉框长目录名溢出**：过长路径顶出下拉框右边界,现改为省略号截断,悬浮可见完整路径。
- **修复派发页流式回复卡顿**：长回复流式输出呈现一块块蹦出而非打字机效果,现合批渲染恢复流畅。
- **修复输出链接点击无反应**：桌面壳内点击输出中的链接曾被同窗导航吞掉,现统一新窗口打开。
- **修复手机远程访问连接失败**：WebSocket 协议写死不安全前缀,HTTPS 访问下被浏览器拦截,现随页面协议动态切换。
- **修复移动端整页可被横向滑走**：长文本撑宽页面,触屏横滑会带走整个页面形似黑屏,现改为纵向收缩并裁切溢出。
- **修复触摸板轻触下拉框首击无响应**：桌面壳内首次轻触常被当作悬停,需点两次才弹出,现改为按下即响应。

## [1.1.0] - 2026-07-15

### 新增
- **定时任务**：一次性与周期任务统一管理,自绘表单快速创建,到点自动开会话执行,接入会话看板与只读回放。
- **定时任务健壮性**：错过触发超 30 分钟标记「已错过」不静默补跑,周期任务连续失败 3 次自动熔断。
- **/resume 命令续接会话**：派发页输入 `/resume` 弹面板选已关闭会话,一键取消隐藏并装载历史原地续接。
- **周回顾与周报草稿**：新增「回顾」视图按周聚合项目与会话热力,一键生成可复制续接的周报草稿。
- **会话标识常驻显示**：输入框上方常驻当前会话名与 id,按所属项目分类色荧光呈现。
- **壁纸背景**：侧栏新增壁纸设置,关闭/壁纸/玻璃三档模式,内置暗色预设与自定义图源。

### 变更
- **完成状态色统一为绿色**：「已完成」状态胶囊由蓝改绿,蓝色专表纯信息,两者不再混用。
- **restart.sh 升级为部署脚本**：重启前自动构建校验、重启后轮询健康检查,任一步失败都给出明确提示。

### 修复
- **会话名被输入内容顶替**：续接会话发送后续消息时,已建立的会话标识会被当次输入短暂覆盖。
- **「待验收」标记 Space 进入不熄灭**：续接视图浏览历史不标已读,现装载历史即标已读。
- **流式输出时无法翻看历史**：消息区曾强制滚底,现仅用户本就在底部时才跟随。
- **本地壁纸图片刷新后丢失**：本地图片改存 IndexedDB,不再受容量限制回退默认壁纸。

### 安全
- **restart.sh 防自斩守卫**：派发会话环境下执行重启脚本立即拒绝,避免误杀宿主后端。

## [1.0.0] - 2026-07-13

璇玑首个正式版本——把散落在 `~/.claude/` 的个人 AI 生产数据聚合成一块本地 Web 驾驶舱,覆盖观测、对话式派发与管理三大职责。

### 新增
- **七视图只读驾驶舱**：仪表盘/项目/会话看板/技能/经验/定时/派发七视图,数字键切换、全键盘可达。
- **对话式任务派发**：经 Agent SDK 流式下发任务,带逐项审批卡、四档权限模式、resume 续接、转后台与打断。
- **会话看板交互**：方向键选卡、Space 进会话、←/Esc 返回,配项目色芯片、限行截断与已完成归档折叠。
- **「待验收」未读标记**：未看过的会话产出点亮琥珀标记并置顶,看过即熄,仪表盘同步催办队列。
- **派发页终端式操作台**：状态行显示项目/分支/模型,自绘主题化下拉,用量三档指示带悬停重置倒计时。
- **派发页命令与富文本**：`/model` 热切换模型、`/rename` 改名,回复按主题渲染 markdown。
- **Token 成本观测**：仪表盘按模型堆叠的成本下钻,区分今日/本周口径并解析会话名显示。
- **项目分类色系统**：等明度只转色相的色环按首次出现持久化到 SQLite,全端「颜色即项目身份」。
- **只读 Adapter 层与 API**：非公开格式解析收敛于 Adapter 层,提供十个只读端点、SQLite 索引与 ws 变更推送。
- **「夜间观象台」设计系统**：以 DESIGN.md 固化 OKLCH token 与六段式规范,实现严格与原型同源。
- **launchd 常驻**：一键安装 LaunchAgent 实现开机自启与异常退出后秒级拉起。
- **后端重启状态还原**：重启后死于重启的会话按「空闲」原地续接,待验收标记与活动摘要一并恢复。
- **侧栏版本号**：logo 旁显示当前版本号 `v1.0.0`。
