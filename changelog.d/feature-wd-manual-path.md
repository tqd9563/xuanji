### 新增

- **/wd 支持手输完整路径**：在切换工作目录弹窗里输入以 `/` 或 `~` 开头的完整路径，列表底部会出现一条「使用此路径」候选，可直接切到从未跑过会话、因而搜不到的全新项目目录。

### 变更

- **/wd 手输路径带存在性校验**：手输行在后端展开 `~` 并校验目标是否为真实目录，校验中与「目录不存在」时该行置灰、Enter 不放行，避免派发到不存在的工作目录。手输行固定置底，输入既有项目路径时 Enter 仍选中既有项目。
