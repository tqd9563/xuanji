### 修复

- **轮次目录跨会话残留**：换会话时轮次索引没有跟着清空，新会话的轮次目录里会列着上一个会话的轮次（实测：lilith_offline_etl 的会话打开目录，前 40 条是另一个 antifraud_skills 会话的提问，本会话自己的轮次被挤到后面编号错位）。现在换会话、接回、交接、清空残留这几条入口统一走 `leaveSession()` 归零，目录只列当前会话的轮次；空会话按 ⌘⇧O 提示「这个会话还没有可跳转的轮次」。
