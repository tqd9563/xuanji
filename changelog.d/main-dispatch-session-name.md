### 变更

- **派发页会话标识**：从会话看板进入某会话后，派发页页头此前不显示会话名，只能靠聊天流里会滚走的 resume 提示辨认；现在页头「派发」标题旁常驻当前窗口绑定会话的 名称 + 项目芯片 + 会话 id（沿用看板卡片同一词汇），随会话生命周期存在：resume 即显示、新会话白纸隐藏、跨目录交接/首条消息先显「未命名会话」、首轮回复结束后回填自动命名；窄屏换行时去掉分隔线。会话名字号 0.8125rem → 0.9375rem，页头一眼可辨。
