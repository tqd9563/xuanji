### 新增

- **卡顿记录器**：前端每 100ms 一次心跳，主线程冻结 ≥500ms 就把现场记下来——当时的视图、最近一次交互（点了哪张卡、走的哪条入口）、还在飞的请求与刚完成请求的耗时，Chromium 上再附带脚本归因；记录落本机 `localStorage`（控制台 `__xuanjiJank()` 可看）并上报后端追加到 `data/client-jank.jsonl`（`GET /api/client-jank` 读取）。用于定位「点进会话偶尔严重卡顿」这类实验室里复现不出来的问题。
