### 新增
- **斜杠命令联想**：派发输入框开头敲 `/` 弹出候选面板，列出璇玑内置命令与本会话 SDK 报告的全部技能（含插件技能如 `/watch:watch`），支持过滤、↑↓ 选择、Tab 补全与参数提示；补全后的命令由 CLI 原生展开为技能调用（原型阶段）。

### 变更
- **派发输入框**：开头完整命中某个已知命令时，命令词在输入框里显示为玉色，Tab 补全后同样保持；未命中的普通消息不受影响。
