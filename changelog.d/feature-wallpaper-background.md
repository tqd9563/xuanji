### 新增

- **壁纸背景**：侧栏底部新增「壁纸」设置——支持关闭/壁纸/玻璃三档模式，内置「星野」（北斗缀夜空）与「山岚」暗色预设，支持本地图片与任意 URL 图源；可调壁纸不透明度（默认 40%）、壁纸模糊（默认 0px）；玻璃档下面板/侧栏/会话卡等全部表面转半透明毛玻璃，表面不透明度（默认 30%）与磨砂强度（默认 0px）独立可调。设置存 localStorage，不写 `~/.claude`；本地大图（>1.5MB dataURL）仅当次会话生效。
