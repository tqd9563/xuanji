### 修复
- **`/wd` 弹窗在正式 App(WKWebView)里搜索框不聚焦**：v1.2.0 的 `/wd` 弹窗打开后光标停留在派发输入框,无法直接打字模糊搜索。根因是「进入派发页自动聚焦输入框」的效应依赖了每次渲染都变化的对象,实际随每次重渲染反复执行,WS 推送/轮询触发的重渲染不断把焦点抢回派发框;浏览器开发环境安静侥幸未复现。现将该聚焦收敛为只在真正进入视图/带意图跳转时执行,`/wd` 拦截时先释放派发框焦点,弹窗内聚焦加轮询兜底(50ms×20,落座即停)。
