### 修复
- **派发会话提问卡不再重复渲染**：Claude 用 `AskUserQuestion` 向用户提问时，之前会在正式的「❓ Claude 提问」卡片上方多渲染一张原始 JSON 工具卡（且 `input` 在 500 字符处硬截断，JSON 语法都不完整），叠加会话框默认贴底滚动，视觉上像是「前面的输出被吞掉，只剩孤立的提问」。现跳过该工具的通用工具卡渲染，只保留一张格式化的提问卡。
