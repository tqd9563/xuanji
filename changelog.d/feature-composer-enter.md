### 变更

- **派发输入框回车语义**：Enter 改为换行（Shift+Enter 同样换行），发送改用 ⌘Enter（非 mac 为 Ctrl+Enter）；底栏提示同步改为「Enter 换行 · ⌘Enter 发送 · ↑↓ 历史」。
- **移除「⚑ 任务总结」按钮**：输入框底栏不再有该按钮，⌘Enter 也不再触发总结；沉淀收口卡改为在输入框输入 `/wrapup`（行为完全不变），回顾页与总结页的空态引导文案同步更新。
