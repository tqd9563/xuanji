### 修复

- **轮次目录时间**：接回/刷新派发会话后，轮次目录（⌘⇧O）与消息头不再把每一轮都标成接回那一刻的时间，改为显示该轮当初提问/回答的真实时刻。后端回放事件现在带上事件发生时刻，前端优先用它而非接收时刻。
- **跨天消息带日期**：轮次目录（⌘⇧O）与消息头的时间，非今天的显示 `MM-DD HH:MM`，今天的仍只显示 `HH:MM`——跨天会话不再出现「昨天 20:09 的下一行是 10:34」这种读着像时间倒流的排列。
