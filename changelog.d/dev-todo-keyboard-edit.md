### 新增

- **待办键盘导航**：待办页支持 `↑ ↓` 移动选中行，`Space` 勾选完成、`Enter` 开工、`E` 改标题、`⌫` 删除；选中行以玉色描边标出并自动滚进视野。
- **待办改标题**：待办标题可就地编辑（双击标题、行尾 ✎ 按钮或按 `E`），回车保存、Esc 放弃、失焦即存；内容没变不打接口。

### 修复

- **勾选框看不清**：待办行首的勾选框描边从 `--line` 换成 `--faint`，在深色底上不再几乎不可见。
