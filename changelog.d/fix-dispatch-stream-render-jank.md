### 修复
- **派发页流式回复卡顿成块**：长回复流式输出时会出现"一块块蹦出来"而非打字机效果，原因是每个文本增量都会重渲染整条消息列表并对着还在增长的全文重新跑一遍 Markdown 解析，渲染跟不上到达速度导致增量在事件队列里排队后成块刷出。改为：增量先合批（每帧只 flush 一次）、历史消息行独立 memo 不再跟着增量重渲染、流式中的消息先用纯文本渲染、回合结束后才一次性切到完整 Markdown。
