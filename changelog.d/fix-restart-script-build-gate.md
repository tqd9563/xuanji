### 变更

- **restart.sh 从裸重启升级为部署脚本**：重启前先同步两端依赖（`--frozen-lockfile`）并构建前端（含 `tsc --noEmit`），任何一步失败即中止不重启——修复「合并 + 重启后端但前端 dist 未重建，新功能不生效」的部署盲区；重启后轮询 `/api/health` 15 秒给出就绪/失败明证，失败时提示真实日志路径（`~/Library/Logs/xuanji/backend.err.log`）。

### 安全

- **restart.sh 加派发会话防自斩守卫**：`XUANJI_DISPATCH=1` 环境下执行立即拒绝并退出（派发会话重启后端 = 杀死自己的宿主，2026-07-09 曾实际发生），守卫已在真实派发会话内实测拦截生效。
