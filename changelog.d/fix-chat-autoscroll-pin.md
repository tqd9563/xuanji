### 修复

- **派发页流式输出中翻历史被拽回底部**：此前消息区在每条流式增量到达时无条件滚底，Claude 持续输出期间向上滚动想看历史上下文，视口会被不断拉回底部，历史根本没法看。现改为「钉底跟随」：仅当用户钉在底部时跟随输出；向上滚动（scrollTop 变小）即解钉、视口冻结，滚回距底 48px 内自动回钉恢复跟随，无需任何按钮。解钉判定用滚动方向而非距底距离——快速流式下程序滚底的 scroll 事件异步到达时内容又已长高，按距离判会误判成用户离开底部而自我解钉（实测复现）。发送消息、续接、接回、新会话、跨目录交接等会话切换动作一律重新钉底。
