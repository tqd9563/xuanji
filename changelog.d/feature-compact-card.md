### 新增

- **回放时间线压缩卡片**：会话回放（会话看板 / 例行任务运行详情）中，`/compact` 或自动压缩的位置现在渲染为一张「上下文已压缩」卡片，显示触发方式（手动/自动）、压缩前 token 数与耗时，点击可展开查看压缩摘要全文。

### 变更

- **压缩摘要不再伪装成用户消息**：此前压缩后续接的摘要记录（isCompactSummary）在回放里显示为一条突兀的英文超长「你」消息，现在回填进压缩卡片，不再混入普通消息流；派发页续接装载历史时同样以一条 `🗜` note 提示压缩点。
