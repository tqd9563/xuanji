### 新增

- **`/resume` 弹窗支持搜索**：「恢复已关闭会话」弹窗头部新增搜索框，打开即聚焦、直接打字过滤，会话名与 sessionId 都参与匹配，命中片段玉色高亮。排序按匹配质量分层（会话名前缀 > 名中段 > 名子序列 > id 命中），同分保持原有的「最近关闭在前」；打分口径与 `/wd`、`/model` 弹窗共用同一套模糊匹配，手感一致。↑↓/Tab 在过滤结果内选择、Enter 续接、Esc 关闭的键盘操作不变，输入时字符键照常落进搜索框。无匹配时提示「没有匹配「x」的已关闭会话」；当前项目一条已关闭会话都没有时不显示搜索框。纯前端过滤，不新增后端接口。
