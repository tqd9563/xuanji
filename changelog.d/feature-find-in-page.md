### 新增

- **会话内查找**：会话回放抽屉与派发聊天页支持 `⌘F`／`Ctrl+F` 查找当前面板里已渲染的内容。查找条钉在面板右上角，`Enter`／`⇧Enter` 跳下一条／上一条，`Esc` 关闭；支持区分大小写与正则两个开关，正则写错时输入框转红而不弹错。折叠的工具卡里的命中照样计入总数，卡头显示「N 处」角标，跳到它时自动展开。派发页只加载最近 200 条历史，查找条会在作用域受限时明示。
