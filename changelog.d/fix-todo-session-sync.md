### 修复
- **待办「继续」丢上下文**：进行中的待办按回车 /「继续 ▶」此前一律按全新派发处理，把标题重发一遍、原会话上下文全丢；现改为续接已挂的会话——后端存活的派发会话直接 attach 接回，已退出的可续接会话 resume 恢复上下文，仅在原会话确实找不回时才回落全新派发（并 toast 提示）。
- **「来自待办」横幅跨会话残留**：从待办开工后若未发送就离开派发页，横幅会一直悬浮且后续无关派发会把该待办错绑到不相干会话；现改为凡不带待办意图进入派发页即清除横幅与关联，带待办意图进入时也会先清掉残留的旧会话状态。

### 变更
- **进行中的待办随会话验收自动完成**：此前待办完成只能手动勾选，会话早已结束的待办会永远停在「进行中」；现在看板刷新时同步——挂靠会话被归档进「已完成」列、或从看板彻底消失（被关闭/转录清理）时，待办自动转「已完成」。会话仅仅跑完（验收中/空闲）仍不算完成，验收处置后才算。
