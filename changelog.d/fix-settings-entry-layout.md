### 修复
- **侧栏设置入口在大字号下挤成竖排**：正文字号设为「大」时整站 rem 文字放大，但侧栏宽度是硬编码的 226px 不跟随，把入口挤到「设置」二字竖排、壁纸状态换行。侧栏宽度改用 `14.125rem`（标准字号下仍是 226px，默认观感不变），随字号等比缩放；同时给「设置」标签加 `flex: none` 退出压缩，状态标签空间不足时省略号截断而非换行，完整值走 `title`。
- **壁纸状态里的「本地图片」缩短为「本地」**：与预设名（星野／山岚）和 URL 一样保持 2 字，最小字号下才不会把后面的档位与百分比挤成省略号。
