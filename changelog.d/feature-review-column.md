### 新增
- **验收中列（原型）**：会话看板新增「验收中」列，跑完的会话默认落入此列，只有显式处置（继续 / 挂起 / 归档）才离开；看过回放只熄灭「待验收」角标、不再挪列，解决已读未处置卡片沉入空闲堆积的问题。

### 变更
- **看板列序与空闲降级（原型）**：列序调整为 运行中 / 等待输入 / 验收中 / 空闲 / 已完成；「空闲」降级为停车场——两行紧凑卡 + 默认折叠（仅展示最近 3 条），注意力让位给验收中。
- **催办队列口径（原型）**：仪表盘「需要你处理」纳入所有验收中会话（含已看未处置的，标记「待处置」），不再只看未读角标。
