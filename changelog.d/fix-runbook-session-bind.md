### 修复

- **验收面板出现时机**：清单改为绑定会话，面板只在「本次交付」的会话里出现。此前只要项目目录下存在 `.xuanji/runbook.json`，同目录的任何新会话一进派发页就弹出验收面板（实测：说「版本发布」、还在被追问版本号时就弹了出来），因为清单不绑会话、也不随验收处置清除。现在清单写了 `sessionId` 就只对该会话渲染；没写则只有写于会话开始之后的才被认领并盖章，早于会话开始的按上次交付的残留处理，面板不出现、清单项也执行不了。
