### 新增

- **会话卡片拖拽归档**：桌面看板上「空闲」列的卡片可直接拖到「已完成」列手动归档；拖拽悬停时目标列显出接纳边框，被拖卡片半透明跟随。运行中/等待输入的卡不可拖(那是真实进行态)，移动端 tab 布局无并排落点故不启用拖拽。
- **归档撤销入口**：已完成列里手动归档的卡片悬停时多一个 ↩ 按钮，点击即回到原状态列。

### 变更

- **归档自动失效**：手动归档的会话一旦重新活跃，卡片自动回到推导出的状态列，不需要手工撤销。判定为「推导态变成运行中/等待输入」或「最近产出时间比归档时前进」——后者兜住「轮询间隙里聊完又回到空闲」这种单看状态会漏判的情况。
