### 修复
- **派发页消息时间**：接回/刷新后 Claude 消息旁的时间不再整片变成「此刻」（同一分钟）。真因在前端合批 delta 时把「事件到达时刻」的读取放进了 React 的 setState 更新函数里，回放几百条事件连发时更新函数延后执行、时刻早已被清空，退回成 Date.now()；现改为先取值再入队。同一模式的轮次计时起点一并修正。
- **前端类型检查**：把两个依赖 node:fs 的扫源码守卫测试从 `src/` 挪到 `tests/`，`pnpm build` 不再因 `tsc --noEmit` 找不到 node 类型而失败。
