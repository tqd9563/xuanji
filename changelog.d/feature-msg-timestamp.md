### 新增

- **消息发送时间**：派发页聊天流与回放抽屉的每条对话消息，角色名后显示发送时间（`HH:MM`，悬停见完整日期时间）。历史消息取 session jsonl 里的原始时间，实时消息在到达时打点。
- **跨天分隔线**：会话跨自然日时插入日期分隔线（如 `2026-08-19 · 周三`），隔夜后台任务与多天续接的长会话可还原时间脉络；会话首条消息前也标一条，交代这段对话发生在哪天。

### 变更

- **工具卡不显示时间**：工具调用与未知事件的源数据本就不带时间戳，不再凭空补时间，也不参与跨天分隔判定。
