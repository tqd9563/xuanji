### 修复

- **看板空格进入**：空格改按物理键位(`e.code`)识别，中文输入法全角空格等场景下不再「按空格没反应、只能回车」；无选中时首按 Space/Enter 直接进入首卡（旧行为是第一下只选中、第二下才进入，容易被误认为按键失效）。
- **看板选中跟随点击**：点击卡片后键盘选中位同步到该卡，此后 Space/Enter 从鼠标停留处继续，不再跳回首卡。
- **「待验收」角标缺失**：盯着派发页跑完的会话，首次进「验收中」列时角标不再缺失——派发页只在回合进行中自动标已读，最后一笔产出留作角标点亮依据，看过回放或续接装载后才熄灭。
