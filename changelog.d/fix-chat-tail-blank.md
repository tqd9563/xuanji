### 修复

- **接回会话后发消息整屏清空**：接回长会话时只先渲染尾部、顶部「显示更早的 N 条」的折叠逻辑（PR#80 引入），会把「发送首条消息开新回合」时后端发来的连接确认误当成回放结束，用当时的全部条数重算隐藏数；若之前用 ⌘⇧O 回填过更早历史，回填条数还会被重复计入，隐藏数超过总条数，整屏只剩「显示更早的 1193 条」一个按钮。现在只有接回回放结束才设定折叠点，折叠点改用稳定行标识锚定（前插历史、追加新消息都不会让它漂移），隐藏数封顶在总条数以内；⌘⇧O 回填或跳到折叠区里的轮次时自动展开。
