### 新增

- **壁纸原型**：设计原型（wiki/design/prototype.html）新增「壁纸背景」演示——侧栏底部入口面板，支持关闭/壁纸/玻璃三档模式、内置「星野」「山岚」暗色预设、本地图片与 URL 图源、不透明度与模糊参数，设置存 localStorage；待原型获批后落地到前端。

### 修复

- **原型默认视图**：无 hash 直接打开 prototype.html 时主区不再空白，默认进入仪表盘视图。
