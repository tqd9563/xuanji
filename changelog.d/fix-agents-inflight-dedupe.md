### 修复

- **看板轮询不再每 5 秒起两个 `claude agents` 子进程**：会话看板与顶部徽章各自以 5 秒轮询 `/api/sessions`，定时器同刻触发时两条请求同时到达后端、缓存双双未命中，各起一个 CLI 子进程（实测 10 秒内 4 个）；用户页面里看到的 `/api/sessions` 永远成对、每次 180–750ms 即此。现在前端同一接口的并发调用合流成一次请求，后端在飞的那次拉取也被并发调用共享——每轮至多一个子进程，整机少一份常驻 CPU 消耗。
