### 修复
- **派发页输入框历史回溯翻回卡死**：↑ 连续回溯到一条含换行的历史 prompt 后，↓ 键会失效、翻不回更新的一条；改为一旦已进入历史浏览态就无条件继续翻，不再被途中某条多行 prompt 卡住。
- **派发页输入框历史回溯误跨会话**：原实现把历史存成浏览器全局 localStorage，导致在别的会话窗口按 ↑ 也能翻出另一个会话发过的 prompt；改为只取当前会话自己的发送记录，新开/续接会话后历史相应清空或替换，不再串会话。
