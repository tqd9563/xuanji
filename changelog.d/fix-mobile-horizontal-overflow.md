### 修复
- **移动端整页可被横向滑走**：仪表盘长单行文本(会话活动摘要中的命令/路径)把文档撑宽数倍,触屏横滑时整个页面滑出视口形似黑屏。三处修复:移动端断点的 `1fr` 网格列改为 `minmax(0, 1fr)` 恢复收缩能力、`.run-item .d` 补 `min-width: 0; flex: 1` 使省略号截断生效、`body` 增加 `overflow-x: clip` 页面级兜底(组件内部横向滚动不受影响)。
