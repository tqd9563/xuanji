### 修复

- **/wd 模糊搜索排序**：修复输入项目名前缀(如 `dee`)时目标项目(deep_baize)被无关项目压在下面的问题。原实现把「短名 + 完整路径」拼成一串做子序列匹配且不排序：公共路径前缀 `/Users/xxx/` 会兜底吸收查询字符,导致几乎所有含首字母的项目都命中,并按原始列表顺序展示。现改为短名与路径分离的分层打分(短名前缀 > 短名连续 > 短名子序列 > 路径连续 > 路径子序列),路径匹配前先剥掉所有候选共有的目录前缀,同分保持原顺序;`/model` 弹窗复用同一组件一并受益,纯模型名选项自动不剥前缀。
