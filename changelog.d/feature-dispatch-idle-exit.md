### 新增

- **派发会话空闲自动退出**：在「验收中 / 空闲」列、没有进行中的回合和旁路提问、且超过设定时间没有任何新输入 / 输出的派发会话，会自动结束 claude 子进程。卡片与记录保留、仍在原列；下次发消息时以 `--resume` 冷启动接上，需几秒，已打开的派发页无需刷新。
- **设置 › 系统监控 › 空闲自动退出**：可选 关闭 / 10 / 30 / 60 / 120 分钟，默认 30，属账户偏好；改动立即生效，调短阈值会当场巡检一次。
- **卡片提示**：已自动退出的会话，卡片右上角数字显示为「—」，悬停提示退出原因，弹窗排行里不再出现它的进程。
