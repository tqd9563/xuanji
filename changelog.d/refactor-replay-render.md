### 变更

- **回放抽屉渲染**：点进会话查看回放时不再一次性挂载整条会话——首屏只渲染前 30 条事件，其余在浏览器空闲时逐片（15 条）补齐；事件列表与各消息卡片改为 memo 缓存，看板 5s 轮询不再重建它们。此前抽屉开着时每次轮询都会把每条助手消息重新走一遍 markdown 解析，实测每次轮询占用主线程 66–630ms（10/10 次非零），改后 8/10 次为 0ms；点进会话到刷出历史上下文的等待相应缩短。
