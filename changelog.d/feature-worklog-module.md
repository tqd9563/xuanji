### 新增

- **「总结」模块**：新增第 9 个视图（侧栏末位，⌘9），只读展示 wrapup skill 落在 `~/.claude/worklog/` 的任务总结卡。列表按日期分组，一行给出项目、任务主题、commit 数与状态胶囊（已合并=绿 / 待合并=蓝 / 未解决=琥珀）；点开抽屉看锚点（分支、commits、MR、外部 refs、covers_until）与「问题 / 结论 / 排除项 / 已知残留 / 关键决策 / 涉及文件」六段正文，正文按 markdown 渲染。**已知残留**单独提为抽屉顶部的琥珀块并计数——它和排除项是卡片里最值钱的两段。会话锚点直连只读回放，不新开会话去问「当时发生了什么」。
- **总结的筛选与搜索**：状态分页签（全部 / 已合并 / 待合并 / 未解决）+ 关键词搜索，搜索范围含结论、排除项、残留正文，不只是标题。移动端在「更多」菜单里带条数提示。

### 变更

- **前端 dev server 后端地址可配**：`XUANJI_PORT` / `XUANJI_WEB_PORT` 可覆盖 vite 代理目标与端口（默认仍是 7777 / 5173）。此前写死 7777，改动验证必须停掉常驻后端才能测；现在可以另起一个实例并存验证，派发会话内尤其需要（不允许重启宿主后端）。
