### 修复

- **隔一阵再打开会话，内容迟迟不出来**：斜杠命令使用频率统计每 10 分钟缓存过期后，下一个请求会同步触发一次全机转录扫描（并发读完近半年全部会话文件并逐个解析），把后端事件循环堵住累计 4–6 秒、单次最长 2.3 秒；这段时间里打开会话的回放、看板、旁路记录等所有请求一起慢 3–4 秒。现在扫描结果按文件缓存、只重读变过的文件（常态约 60ms），扫描改为逐个文件处理并在文件间让出事件循环（冷扫描时单次阻塞 52ms），缓存过期时先返回旧值、后台单飞刷新，请求不再等扫描。
